{
  "name": "mise-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Self-contained Playwright runner for mise's dockerized e2e suite.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
